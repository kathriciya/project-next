import cls from './footer.module.css';

const Footer = () => {
  return (
    <footer className={`container ${cls.footer}`}>
      Created by &copy;kathriciya
    </footer>
  );
};

export default Footer;
